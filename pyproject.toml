[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lmutual"
version = "0.2.1"
description = "Collective intelligence & witness protocol"
dependencies = ["pytest>=7", "jsonschema>=4", "pyyaml>=6"]

[tool.hatch.build.targets.wheel]
packages = ["."]
